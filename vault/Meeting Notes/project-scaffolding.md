# Project Scaffolding

## Overview
פרויקט Sticker — מערכת צוות סוכנים ליצירת מדבקות לבית הספר. הצוות: **לולי** (המנכ"לית, מיוצגת ע"י `CLAUDE.md` בשורש, מנתבת בקשות), **מיטל** (כותבת תוכן ועימוד), **עמרי** (מעצב תמונות). נושא זה מתעד את הקמת התשתית הבסיסית: מבנה התיקיות תחת `.claude/` (`agents/`, `skills/`) וקובץ `CLAUDE.md` הראשוני. הגדרות הסוכנים עצמם ו-routing יתווספו בהמשך.

## Open Questions
- האם וכיצד לחבר את הפרויקט ל-repo בGitHub (https://github.com/meytalcairo-OMG/Sticker) — git עדיין לא אותחל, ותיקיות ריקות לא יידחפו ללא תוכן/`.gitkeep`.
- התוכן של `agents/` ו-`skills/` (הגדרות מיטל ועמרי, יכולות) טרם הוגדר.

## Session Log

### 2026-06-24 — הקמת תשתית בסיסית [shipped]
- **What was done:** נוצרו `.claude/agents/` ו-`.claude/skills/` (ריקות), ונוצר `CLAUDE.md` בשורש המתפקד כלולי — הצגה עצמית בגוף ראשון, תיאור הפרויקט, רשימת צוות, מבנה תיקיות והערה שזהו קובץ ראשוני.
- **Decisions:** התיקיות נשארו ריקות ללא `.gitkeep` לפי בקשה מפורשת. לא נכתב קוד ולא נבנו סוכנים — רק תשתית. לא בוצע git init/push (המשתמש ציין שפתח repo אך לא ביקש לחבר).
- **Notes / Caveats:** git לא עוקב אחרי תיקיות ריקות — יידרש תוכן או `.gitkeep` כדי שהן יידחפו לGitHub. הפרויקט מכיל גם `פרומפטים.rtf` (קובץ פרומפטים מקורי, לא נקרא בסשן זה).
- **Related:** none (first entry on this topic)
